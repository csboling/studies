\message{ !name(permutation-groups.tex)}\documentclass{article}

\include{preamble}

\begin{document}

\message{ !name(permutation-groups.tex) !offset(240) }
$\{i,j\}$ is disjoint from $\{r,s\}$ then
    $$
    (i~j)(r~s) = (i~j~r)(j~r~s).
    $$

  }
  \item{
  }
\end{enumerate}
\end{proof}

\end{document}
\message{ !name(permutation-groups.tex) !offset(-20) }
